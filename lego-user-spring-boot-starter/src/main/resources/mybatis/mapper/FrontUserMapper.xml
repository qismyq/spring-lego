<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.yunqihui.starter.user.mapper.FrontUserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="net.yunqihui.autoconfigure.user.entity.FrontUser">
        <id column="id" property="id" />
        <result column="account" property="account" />
        <result column="wechatId" property="wechatId" />
        <result column="unionId" property="unionId" />
        <result column="alipayId" property="alipayId" />
        <result column="password" property="password" />
        <result column="del" property="del" />
        <result column="role" property="role" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, account, wechatId, unionId, alipayId, password, del, role
    </sql>

</mapper>
