<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.yunqihui.starter.user.mapper.ConfigurationMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="net.yunqihui.starter.common.entity.Configuration">
        <id column="code" property="code" />
        <result column="value" property="value" />
        <result column="descr" property="descr" />
        <result column="chooseFlag" property="chooseFlag" />
        <result column="id" property="id" />
        <result column="type" property="type" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        code, value, descr, chooseFlag, id, type
    </sql>

</mapper>
